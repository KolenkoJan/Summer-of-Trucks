export interface IEvent {
    id: string // firebase ID
    index: number
    title: string
    description: string
    startDate: string
    address: string
    latitude: number
    longitude: number
}
